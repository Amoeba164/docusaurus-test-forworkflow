# 🔄 Автоматическая синхронизация файловой системы

## 🎯 Что это даёт

Теперь **любой MD/MDX файл в репозитории** автоматически появляется в Docusaurus!

### Было:
```
1. Создали файл где-то в репо
2. Вручную скопировали в docs/
3. Обновили sidebars.js
4. Добавили frontmatter
```

### Стало:
```
1. Создали файл где-то в репо
2. ✨ Готово! Всё автоматически!
```

---

## 🚀 Как это работает

### Автоматическая синхронизация

```
Любой файл .md или .mdx в репо (кроме системных папок)
                    ↓
        [sync-filesystem.js]
                    ↓
    Копируется в docs/ с той же структурой
                    ↓
    Автоматически добавляется frontmatter
                    ↓
    Создаются index.md для папок
                    ↓
    Генерируется sidebars.js
                    ↓
        Docusaurus показывает!
```

### Системные папки (исключаются)

Эти папки **НЕ** синхронизируются:
- `node_modules/`
- `.git/`
- `.github/`
- `build/`
- `.docusaurus/`
- `static/`
- `src/`

Можно настроить в `sync-filesystem.js`

---

## 📝 Использование

### Вариант 1: Ручная синхронизация

```bash
# Синхронизировать один раз
npm run sync

# Результат:
# ✓ Все MD файлы скопированы в docs/
# ✓ Frontmatter добавлен
# ✓ sidebars.js сгенерирован
```

**Когда использовать:**
- Перед коммитом
- Периодически для обновления

---

### Вариант 2: Watch режим (локально)

```bash
# Запустить watch + Docusaurus
npm run dev:watch

# Что происходит:
# 1. Первоначальная синхронизация
# 2. Запускается Docusaurus
# 3. Отслеживает изменения файлов
# 4. Автоматически ресинхронизирует
```

**Когда использовать:**
- При активной разработке
- Когда часто создаёте/редактируете файлы

---

### Вариант 3: GitHub Actions (автоматически)

При каждом `push` в `main`:
1. GitHub Actions запускает `sync-filesystem.js`
2. Коммитит изменения в `docs/` и `sidebars.js`
3. Деплоит на GitHub Pages

**Настройка:**
Скопируйте `.github/workflows/auto-sync-filesystem.yml` в ваш репо

---

## 🎨 Примеры использования

### Пример 1: Создать новую папку с документацией

```bash
# Создайте папку где угодно
mkdir analytics

# Добавьте файлы
echo "# Аналитика продаж" > analytics/sales.md
echo "# Аналитика маркетинга" > analytics/marketing.md

# Синхронизируйте
npm run sync

# Результат в Docusaurus:
# docs/
# └── analytics/
#     ├── index.md (создан автоматически)
#     ├── sales.md (с frontmatter)
#     └── marketing.md (с frontmatter)
```

### Пример 2: Структура из нескольких уровней

```bash
# Создайте вложенную структуру
mkdir -p projects/2024/q4
echo "# Проект А" > projects/2024/q4/project-a.md
echo "# Проект Б" > projects/2024/q4/project-b.md

npm run sync

# Результат в Docusaurus:
# docs/
# └── projects/
#     ├── index.md
#     └── 2024/
#         ├── index.md
#         └── q4/
#             ├── index.md
#             ├── project-a.md
#             └── project-b.md
```

### Пример 3: Добавить файл в существующую структуру

```bash
# Просто создайте файл
echo "# Новый гайд" > guides/new-guide.md

# В watch режиме синхронизируется автоматически
# Или вручную:
npm run sync
```

---

## ⚙️ Конфигурация

### Настройка исключений

Отредактируйте `sync-filesystem.js`:

```javascript
const config = {
  // Добавьте папки для исключения
  excludeDirs: [
    'node_modules',
    '.git',
    'my-private-folder',  // ← добавьте свою
  ],
  
  // Добавьте файлы для исключения
  excludeFiles: [
    'package.json',
    'secret-notes.md',  // ← добавьте свой
  ],
  
  // Какие расширения синхронизировать
  includeExtensions: [
    '.md',
    '.mdx',
    // '.txt',  // можно добавить
  ],
  
  // Автоматически добавлять frontmatter
  addFrontmatter: true,
  
  // Создавать index.md для папок
  createIndexFiles: true,
};
```

### Настройка frontmatter

По умолчанию генерируется:

```yaml
---
title: Название Файла
sidebar_position: auto
---
```

Можно изменить в функции `generateFrontmatter()` в `sync-filesystem.js`

---

## 🔍 Как проверить что синхронизировано

### После синхронизации

```bash
npm run sync

# Проверьте docs/
ls -R docs/

# Проверьте sidebars.js
cat sidebars.js

# Запустите Docusaurus
npm start
```

### Логи синхронизации

Скрипт выводит:
```
✓ Synced: path/to/file.md
⊘ Skipped dir: node_modules
✓ Created index: docs/folder/index.md
✓ Generated sidebars.js
```

---

## 🎯 Workflow команды

### Сценарий 1: Новый контрибьютор

```bash
# 1. Клонируют репо
git clone https://github.com/Amoeba164/high-time-workflow.git
cd high-time-workflow

# 2. Устанавливают зависимости
npm install

# 3. Запускают с auto-sync
npm run dev:watch

# 4. Создают файлы где угодно
mkdir my-docs
echo "# Мой документ" > my-docs/doc.md

# 5. Автоматически видят в Docusaurus!
```

### Сценарий 2: Быстрая правка

```bash
# 1. Редактируют файл напрямую в корне
vim important-guide.md

# 2. Синхронизируют
npm run sync

# 3. Коммитят
git add .
git commit -m "Обновил гайд"
git push

# 4. GitHub Actions автоматически деплоит
```

### Сценарий 3: Реорганизация структуры

```bash
# 1. Перемещаете файлы как хотите
mv old-folder/doc.md new-folder/doc.md

# 2. Синхронизируете
npm run sync

# 3. Структура в docs/ автоматически обновится!
```

---

## 🐛 Troubleshooting

### Проблема: Файл не появляется в Docusaurus

**Решение:**
```bash
# 1. Проверьте что это .md или .mdx
file your-file.md

# 2. Проверьте что папка не в excludeDirs
cat sync-filesystem.js | grep excludeDirs

# 3. Запустите sync с логами
npm run sync

# 4. Проверьте docs/
ls -la docs/
```

### Проблема: Watch не работает

**Решение:**
```bash
# 1. Убедитесь что chokidar установлен
npm install chokidar --save-dev

# 2. Проверьте что нет ошибок
npm run sync:watch

# 3. Попробуйте пересоздать node_modules
rm -rf node_modules
npm install
```

### Проблема: Конфликт с существующими docs/

**Решение:**
```bash
# 1. Сделайте бэкап
cp -r docs/ docs-backup/

# 2. Запустите sync
npm run sync

# 3. Проверьте результат
# 4. Восстановите если нужно
```

### Проблема: Frontmatter перезаписывается

**Решение:**

Скрипт **НЕ** перезаписывает существующий frontmatter!

Если файл уже имеет frontmatter (начинается с `---`), он сохраняется.

---

## 📊 Сравнение подходов

| Подход | Плюсы | Минусы | Когда использовать |
|--------|-------|--------|-------------------|
| **Ручной sync** | Полный контроль | Нужно помнить запускать | Периодические обновления |
| **Watch режим** | Автоматически | Нужно держать запущенным | Активная разработка |
| **GitHub Actions** | Полная автоматизация | Задержка на CI/CD | Production деплой |

---

## ✅ Чек-лист настройки

- [ ] Скопировать `sync-filesystem.js` в корень репо
- [ ] Скопировать `sync-filesystem-watch.js` в корень репо
- [ ] Обновить `package.json` с новыми скриптами
- [ ] Установить `chokidar`: `npm install chokidar --save-dev`
- [ ] Установить `concurrently`: `npm install concurrently --save-dev`
- [ ] Скопировать GitHub Action в `.github/workflows/`
- [ ] Настроить исключения в `sync-filesystem.js`
- [ ] Запустить первую синхронизацию: `npm run sync`
- [ ] Проверить результат: `npm start`
- [ ] Протестировать watch: `npm run dev:watch`

---

## 🎉 Готово!

Теперь вся ваша файловая система автоматически синхронизируется с Docusaurus!

**Создали файл где-то → он появляется в docs → Docusaurus показывает → автодеплой!**

---

## 💡 Best Practices

### ✅ DO:
- Создавайте логичную структуру папок
- Используйте понятные имена файлов
- Добавляйте README.md в папки (станет index.md)
- Запускайте `npm run sync` перед коммитом

### ❌ DON'T:
- Не создавайте файлы в системных папках
- Не используйте спецсимволы в именах файлов
- Не редактируйте напрямую docs/ (будет перезаписано)
- Не забывайте про .gitignore

---

## 🔗 Связанные документы

- [SYSTEM.md](SYSTEM.md) - Полная система workflow
- [SETUP.md](SETUP.md) - Первоначальная установка
- [DEPLOYMENT.md](DEPLOYMENT.md) - Деплой и CI/CD

---

**Вопросы?** Создайте Issue или спросите в команде!
