# 📦 Версионирование документации

## Зачем нужно версионирование?

Версионирование позволяет:
- ✅ Сохранить старые версии документации для пользователей
- ✅ Разрабатывать новую версию не затрагивая текущую
- ✅ Показывать изменения между версиями
- ✅ Дать пользователям выбирать какую версию смотреть

## Когда создавать новую версию?

Создавайте новую версию когда:
- 🎯 Завершён крупный набор изменений
- 🎯 Готовы зафиксировать текущее состояние
- 🎯 Хотите начать работать над следующей версией отдельно

**Не создавайте версию для каждого мелкого изменения!**

## Как создать версию

### Шаг 1: Убедитесь что текущая документация готова

```bash
# Проверьте что всё работает локально
npm start

# Проверьте сборку
npm run build
```

### Шаг 2: Создайте версию

```bash
# Создать версию с номером (например, 1.0)
npm run version 1.0

# Или с датой
npm run version 2024-12

# Или по семантической версии
npm run version 1.2.0
```

### Шаг 3: Что происходит после команды?

Docusaurus автоматически:
1. Создаёт `versioned_docs/version-1.0/` с копией всех docs
2. Создаёт `versioned_sidebars/version-1.0-sidebars.json` с копией сайдбара
3. Обновляет `versions.json` добавляя новую версию

### Шаг 4: Закоммитьте изменения

```bash
git add .
git commit -m "Release version 1.0"
git push origin main
```

## Структура после версионирования

```
high-time-workflow/
├── docs/                              # Текущая версия (Next)
│   ├── intro.md
│   └── lessons/
│
├── versioned_docs/
│   ├── version-1.0/                  # Версия 1.0 (замороженная)
│   │   ├── intro.md
│   │   └── lessons/
│   └── version-2.0/                  # Версия 2.0 (замороженная)
│       ├── intro.md
│       └── lessons/
│
├── versioned_sidebars/
│   ├── version-1.0-sidebars.json
│   └── version-2.0-sidebars.json
│
└── versions.json                      # ["2.0", "1.0"]
```

## Настройка версий в docusaurus.config.js

Уже настроено:

```javascript
docs: {
  lastVersion: 'current',
  versions: {
    current: {
      label: 'Текущая версия',
      path: '/',
    },
  },
}
```

### Кастомизация label'ов версий

```javascript
versions: {
  current: {
    label: 'Next 🚧',
    path: 'next',
    banner: 'unreleased',
  },
  '2.0': {
    label: '2.0',
    path: '/',
  },
  '1.0': {
    label: '1.0',
    path: 'v1',
    banner: 'unmaintained',
  },
}
```

## Работа с разными версиями

### Редактировать текущую версию (Next)
```bash
# Редактируйте файлы в docs/
vim docs/lessons/lesson-0.md
```

### Редактировать старую версию
```bash
# Редактируйте файлы в versioned_docs/
vim versioned_docs/version-1.0/lessons/lesson-0.md
```

**⚠️ Важно:** Изменения в старых версиях нужны редко! Обычно старые версии не меняются.

## URL структура

После версионирования:
- **Текущая версия:** `https://...github.io/high-time-workflow/`
- **Версия 2.0:** `https://...github.io/high-time-workflow/`
- **Версия 1.0:** `https://...github.io/high-time-workflow/v1/`

## Переключение версий на сайте

В навигационной панели появится dropdown "Версии":
```
Версии ▼
  ✓ Текущая версия
    2.0
    1.0
```

## Удаление версии

```bash
# 1. Удалите папку версии
rm -rf versioned_docs/version-1.0/
rm -f versioned_sidebars/version-1.0-sidebars.json

# 2. Удалите из versions.json
# Откройте versions.json и уберите "1.0"

# 3. Закоммитьте
git add .
git commit -m "Remove version 1.0"
git push
```

## Best Practices

### ✅ DO:
- Создавайте версию когда готовы к "релизу"
- Используйте понятные номера версий (1.0, 2.0, 2024-Q4)
- Документируйте что изменилось между версиями
- Держите 2-3 последние версии, старые удаляйте

### ❌ DON'T:
- Не создавайте версию для каждого коммита
- Не редактируйте старые версии без крайней необходимости
- Не используйте сложные схемы версионирования

## Пример workflow с версиями

### Этап 1: Разработка (docs/)
```
Работаем над новым контентом в docs/
Делаем коммиты, PR, исправления
```

### Этап 2: Готовы к релизу
```bash
npm run version 1.0
git add .
git commit -m "Release v1.0"
git push
```

### Этап 3: Продолжаем работу
```
Новые изменения идут в docs/ (будущая v2.0)
Версия 1.0 заморожена в versioned_docs/version-1.0/
```

### Этап 4: Следующий релиз
```bash
npm run version 2.0
# И так далее...
```

## Changelog

Рекомендуется создать CHANGELOG.md для трекинга изменений:

```markdown
# Changelog

## [2.0] - 2025-01-15
### Added
- Новый урок 5
- Интерактивные тесты

### Changed
- Переработан урок 1

## [1.0] - 2024-12-01
### Added
- Первый релиз
- Уроки 0-4
```

## Вопросы и ответы

**Q: Как часто создавать версии?**  
A: Раз в месяц/квартал, когда есть существенные изменения

**Q: Можно ли не использовать версионирование?**  
A: Да! Если контент постоянно обновляется, версионирование не обязательно

**Q: Сколько версий держать?**  
A: 2-3 последние. Старые версии интересны очень редко

**Q: Как показать баннер "устаревшая версия"?**  
A: См. пример с `banner: 'unmaintained'` выше

---

**Готовы создать первую версию?** Следуйте инструкциям выше! 🚀
