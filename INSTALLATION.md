# 🏗️ Полный гайд: Создание репозитория High Time Workflow с нуля

## 📋 Содержание

1. [Подготовка](#подготовка)
2. [Создание репозитория](#создание-репозитория)
3. [Структура проекта](#структура-проекта)
4. [Установка файлов](#установка-файлов)
5. [Настройка GitHub](#настройка-github)
6. [Первый запуск](#первый-запуск)
7. [Добавление контента](#добавление-контента)
8. [Настройка команды](#настройка-команды)

---

## 🎯 Подготовка

### Что вам нужно

- ✅ GitHub аккаунт
- ✅ Git установлен локально
- ✅ Node.js 18+ установлен
- ✅ Текстовый редактор (VS Code рекомендуется)
- ✅ Терминал (bash/zsh/PowerShell)

### Проверка установки

```bash
# Проверьте Git
git --version
# Должно быть: git version 2.x.x

# Проверьте Node.js
node --version
# Должно быть: v18.x.x или выше

# Проверьте npm
npm --version
# Должно быть: 9.x.x или выше
```

---

## 🆕 Создание репозитория

### Шаг 1: Создайте репо на GitHub

1. Перейдите на https://github.com
2. Нажмите "New repository" (зелёная кнопка)
3. Заполните:
   - **Repository name:** `high-time-workflow`
   - **Description:** `Документация и workflow для команды High Time`
   - **Visibility:** Public (или Private если нужно)
   - ✅ Поставьте галочку "Add a README file"
   - ✅ Добавьте .gitignore → выберите "Node"
4. Нажмите "Create repository"

### Шаг 2: Клонируйте локально

```bash
# Перейдите в папку где хотите работать
cd ~/Documents

# Клонируйте репозиторий
git clone https://github.com/ваш-username/high-time-workflow.git

# Перейдите в папку
cd high-time-workflow

# Проверьте что всё ок
ls -la
# Должны увидеть: .git, README.md, .gitignore
```

---

## 📁 Структура проекта

### Финальная структура (к чему стремимся)

```
high-time-workflow/
│
├── 📁 .github/                          # GitHub конфигурация
│   ├── workflows/                       # GitHub Actions
│   │   ├── deploy.yml                  # Деплой на Pages
│   │   └── auto-sync-filesystem.yml    # Автосинхронизация
│   ├── CODEOWNERS                      # Ownership файлов
│   └── ISSUE_TEMPLATE/                 # Шаблоны Issues
│       └── task.yml
│
├── 📁 docs/                             # Вся документация (генерируется автоматически)
│   ├── intro.md                        # Главная страница
│   ├── lessons/                        # Уроки
│   │   ├── lesson-0.md
│   │   ├── lesson-1.md
│   │   └── ...
│   ├── analysis/                       # Анализ
│   │   └── course-analysis.md
│   └── workflows/                      # Процессы
│       └── team-workflow.md
│
├── 📁 src/                              # Кастомизация Docusaurus
│   ├── css/                            # Стили
│   │   └── custom.css
│   ├── components/                     # React компоненты (будущее)
│   └── pages/                          # Кастомные страницы (будущее)
│
├── 📁 static/                           # Статические файлы
│   ├── img/                            # Изображения
│   │   ├── favicon.ico
│   │   └── logo.svg
│   └── files/                          # Другие файлы
│
├── 📁 versioned_docs/                   # Версии документации (создаётся при версионировании)
├── 📁 versioned_sidebars/               # Версии сайдбаров (создаётся при версионировании)
│
├── 📄 docusaurus.config.js              # Главная конфигурация Docusaurus
├── 📄 sidebars.js                       # Конфигурация навигации (генерируется автоматически)
├── 📄 package.json                      # Зависимости и скрипты
├── 📄 package-lock.json                 # Lockfile зависимостей
├── 📄 versions.json                     # Список версий
│
├── 📄 sync-filesystem.js                # Скрипт автосинхронизации
├── 📄 sync-filesystem-watch.js          # Watch режим
│
├── 📄 README.md                         # Основная документация проекта
├── 📄 SETUP.md                          # Гайд по установке
├── 📄 SYSTEM.md                         # Система workflow
├── 📄 DEPLOYMENT.md                     # Инструкции по деплою
├── 📄 VERSIONING.md                     # Работа с версиями
├── 📄 AUTO-SYNC-GUIDE.md               # Гайд по автосинхронизации
│
├── 📄 .gitignore                        # Что не коммитить
├── 📄 .npmrc                            # npm конфигурация (опционально)
└── 📄 babel.config.js                   # Babel конфигурация (создаётся автоматически)
```

---

## 🔧 Установка файлов

### Шаг 1: Установите базовые зависимости

```bash
# Инициализируйте npm проект (если ещё не сделали)
npm init -y

# Установите Docusaurus
npm install @docusaurus/core @docusaurus/preset-classic

# Установите React (требуется для Docusaurus)
npm install react react-dom

# Установите dev зависимости
npm install --save-dev @docusaurus/module-type-aliases @docusaurus/types typescript

# Установите зависимости для автосинхронизации
npm install --save-dev chokidar concurrently
```

### Шаг 2: Создайте базовую структуру папок

```bash
# Создайте основные папки
mkdir -p docs/lessons docs/analysis docs/workflows
mkdir -p src/css src/components src/pages
mkdir -p static/img static/files
mkdir -p .github/workflows .github/ISSUE_TEMPLATE

# Проверьте структуру
tree -L 2
```

### Шаг 3: Скопируйте файлы из архивов

#### А. Из `complete-workflow-system.tar.gz`:

```bash
# Распакуйте архив
tar -xzf complete-workflow-system.tar.gz

# Скопируйте Docusaurus setup
cp -r complete-workflow-system/docusaurus-setup/* ./

# Скопируйте документацию
cp complete-workflow-system/SYSTEM.md ./
cp complete-workflow-system/README-START-HERE.md ./README.md

# Скопируйте Git гайды в docs
cp -r complete-workflow-system/git-guides/* docs/workflows/

# Скопируйте GitHub templates
cp -r complete-workflow-system/github-templates/.github/* .github/
```

#### Б. Из `auto-sync-system.tar.gz`:

```bash
# Распакуйте архив
tar -xzf auto-sync-system.tar.gz

# Скопируйте скрипты синхронизации
cp auto-sync-system/sync-filesystem.js ./
cp auto-sync-system/sync-filesystem-watch.js ./

# Скопируйте GitHub Action
cp auto-sync-system/github-actions-sync.yml .github/workflows/

# Скопируйте документацию
cp auto-sync-system/AUTO-SYNC-GUIDE.md ./

# Обновите package.json (возьмите scripts секцию из auto-sync-system/package.json)
```

### Шаг 4: Обновите package.json

Откройте `package.json` и добавьте/обновите секцию scripts:

```json
{
  "scripts": {
    "docusaurus": "docusaurus",
    "start": "docusaurus start",
    "build": "docusaurus build",
    "swizzle": "docusaurus swizzle",
    "deploy": "docusaurus deploy",
    "clear": "docusaurus clear",
    "serve": "docusaurus serve",
    "write-translations": "docusaurus write-translations",
    "write-heading-ids": "docusaurus write-heading-ids",
    "typecheck": "tsc",
    "version": "docusaurus docs:version",
    
    "sync": "node sync-filesystem.js",
    "sync:watch": "node sync-filesystem-watch.js",
    "dev": "npm run sync && npm start",
    "dev:watch": "concurrently \"npm run sync:watch\" \"npm start\"",
    "prebuild": "npm run sync"
  }
}
```

### Шаг 5: Настройте docusaurus.config.js

Откройте `docusaurus.config.js` и обновите:

```javascript
const config = {
  title: 'High Time Workflow',
  tagline: 'Документация по процессам и экспериментам',
  favicon: 'img/favicon.ico',

  // ВАЖНО: Обновите эти значения под ваш GitHub
  url: 'https://ваш-username.github.io',
  baseUrl: '/high-time-workflow/',
  
  organizationName: 'ваш-username',
  projectName: 'high-time-workflow',
  
  // Остальное оставьте как есть
  // ...
};
```

### Шаг 6: Создайте .gitignore

Убедитесь что `.gitignore` содержит:

```
# Dependencies
/node_modules
/.pnp
.pnp.js

# Production
/build

# Generated files
.docusaurus
.cache-loader

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDEs
.idea/
.vscode/
*.swp
*.swo

# OS
Thumbs.db
```

---

## 📝 Детальная структура по папкам

### .github/

```
.github/
├── workflows/
│   ├── deploy.yml                 # Деплой на GitHub Pages
│   │   └── Содержит: build + deploy workflow
│   └── auto-sync-filesystem.yml   # Автосинхронизация
│       └── Содержит: sync + commit + deploy workflow
│
├── CODEOWNERS                     # Ownership файлов
│   └── Формат: /путь/к/файлу @username
│
└── ISSUE_TEMPLATE/
    └── task.yml                   # Шаблон для Issues
        └── Содержит: dropdown'ы, поля, чеклисты
```

**Зачем:**
- `workflows/` - автоматизация CI/CD
- `CODEOWNERS` - автоматические ревьюеры на PR
- `ISSUE_TEMPLATE/` - стандартизация Issues

---

### docs/

```
docs/
├── intro.md                       # Главная страница сайта
│   └── Первое что видит пользователь
│
├── lessons/                       # Уроки курса
│   ├── lesson-0.md               # Когда оптимизация не работает
│   ├── lesson-1.md               # Эксперименты с путём к ценности
│   ├── lesson-2.md               # Эксперименты с каналами
│   ├── lesson-3.md               # Эксперименты с продажами
│   └── lesson-4.md               # Система экспериментов
│
├── analysis/                      # Аналитические материалы
│   ├── course-analysis.md        # JTBD, ЦА, value propositions
│   └── target-audience.md        # Детальное описание ЦА
│
└── workflows/                     # Процессы команды
    ├── team-workflow.md          # Общий workflow
    ├── git-basics.md             # Git для начинающих
    └── git-branches.md           # Работа с ветками
```

**Зачем:**
- Вся документация в одном месте
- Автоматическая навигация
- Структура = навигация в Docusaurus

**Важно:**
- Каждый `.md` файл должен иметь frontmatter
- Названия файлов влияют на URL

---

### src/

```
src/
├── css/
│   └── custom.css                 # Кастомные стили
│       └── Переменные, темы, классы
│
├── components/                    # React компоненты (будущее)
│   ├── InteractiveTest.jsx       # Интерактивные тесты
│   ├── Dashboard.jsx             # Дашборды
│   └── JTBDAnalyzer.jsx          # Анализаторы
│
└── pages/                         # Кастомные страницы (будущее)
    ├── index.js                  # Кастомная главная (опционально)
    └── about.md                  # О проекте (опционально)
```

**Зачем:**
- Кастомизация внешнего вида
- Интерактивные компоненты
- Специальные страницы вне docs/

**Когда использовать:**
- Для уникального дизайна
- Для React компонентов
- Для landing pages

---

### static/

```
static/
├── img/
│   ├── favicon.ico               # Иконка сайта (32x32px)
│   ├── logo.svg                  # Логотип для навигации
│   ├── docusaurus-social-card.jpg # OpenGraph (1200x630px)
│   └── screenshots/              # Скриншоты для документации
│       ├── workflow-diagram.png
│       └── dashboard-example.png
│
└── files/                        # Другие файлы для скачивания
    ├── templates/
    │   └── project-template.xlsx
    └── examples/
        └── sample-data.csv
```

**Зачем:**
- Статические ресурсы
- Изображения в документации
- Файлы для скачивания

**Использование в MD:**
```markdown
![Диаграмма](/img/screenshots/workflow-diagram.png)

[Скачать шаблон](/files/templates/project-template.xlsx)
```

---

### Корневые конфигурационные файлы

```
📄 docusaurus.config.js            # Главная конфигурация
   └── URL, baseURL, темы, плагины, навигация

📄 sidebars.js                      # Структура навигации
   └── Генерируется автоматически sync-filesystem.js

📄 package.json                     # Зависимости и скрипты
   └── npm install, npm start, npm run sync

📄 versions.json                    # Список версий документации
   └── ["2.0", "1.0"] - создаётся при версионировании

📄 babel.config.js                  # Babel конфигурация
   └── Создаётся автоматически Docusaurus
```

---

### Скрипты автосинхронизации

```
📄 sync-filesystem.js               # Основной скрипт
   └── Сканирует репо → копирует в docs/

📄 sync-filesystem-watch.js         # Watch режим
   └── Отслеживает изменения → автосинхронизация
```

**Использование:**
```bash
# Ручная синхронизация
npm run sync

# Watch режим
npm run sync:watch

# Dev с автосинхронизацией
npm run dev:watch
```

---

### Документационные файлы

```
📄 README.md                        # Основная документация
   └── Что это, как установить, как использовать

📄 SETUP.md                         # Пошаговая установка
   └── От нуля до работающего сайта

📄 SYSTEM.md                        # Полная система workflow
   └── Роли, процессы, платформы, метрики

📄 DEPLOYMENT.md                    # Инструкции по деплою
   └── GitHub Pages, troubleshooting

📄 VERSIONING.md                    # Работа с версиями
   └── Как создавать версии, когда, зачем

📄 AUTO-SYNC-GUIDE.md              # Гайд по автосинхронизации
   └── Как работает, примеры, настройка
```

---

## ⚙️ Настройка GitHub

### Шаг 1: Настройте GitHub Pages

1. Перейдите в Settings → Pages
2. Source: выберите "Deploy from a branch"
3. Branch: выберите `gh-pages` (появится после первого деплоя)
4. Folder: `/ (root)`
5. Save

### Шаг 2: Настройте права для GitHub Actions

1. Settings → Actions → General
2. Workflow permissions:
   - Выберите: **"Read and write permissions"**
   - Поставьте галочку: **"Allow GitHub Actions to create and approve pull requests"**
3. Save

### Шаг 3: Обновите CODEOWNERS

Откройте `.github/CODEOWNERS` и замените username'ы на реальные:

```
# Было:
/docs/lessons/lesson-0.md @product-manager

# Стало (ваш реальный GitHub username):
/docs/lessons/lesson-0.md @real-github-username
```

---

## 🚀 Первый запуск

### Шаг 1: Установите зависимости

```bash
# Убедитесь что вы в корне проекта
pwd
# Должно показать: /path/to/high-time-workflow

# Установите всё
npm install

# Это займёт 1-2 минуты
```

### Шаг 2: Запустите синхронизацию

```bash
# Синхронизируйте файловую систему
npm run sync

# Должны увидеть логи:
# ✓ Synced: lessons/lesson-0.md
# ✓ Created index: docs/lessons/index.md
# ✓ Generated sidebars.js
# ✅ Filesystem sync complete!
```

### Шаг 3: Запустите Docusaurus локально

```bash
# Запустите dev server
npm start

# Должен открыться браузер на http://localhost:3000
# Если нет, откройте вручную
```

### Шаг 4: Проверьте что всё работает

В браузере проверьте:
- ✅ Главная страница открывается
- ✅ Навигация слева работает
- ✅ Уроки видны
- ✅ Поиск работает (если настроен)

### Шаг 5: Первый коммит

```bash
# Добавьте все файлы
git add .

# Закоммитьте
git commit -m "Initial Docusaurus setup with auto-sync"

# Запушьте
git push origin main
```

### Шаг 6: Дождитесь первого деплоя

1. Перейдите на GitHub → Actions
2. Увидите running workflow "Auto Sync Filesystem to Docs"
3. Дождитесь завершения (2-3 минуты)
4. Проверьте что ветка `gh-pages` создалась

### Шаг 7: Проверьте live сайт

Откройте: `https://ваш-username.github.io/high-time-workflow/`

Если показывает 404:
- Подождите 5-10 минут
- Проверьте Settings → Pages настройки
- Проверьте что ветка `gh-pages` есть

---

## 📝 Добавление контента

### Способ 1: Через автосинхронизацию (рекомендуется)

```bash
# 1. Создайте файл где угодно в репо
mkdir team-guides
echo "# Наш workflow" > team-guides/workflow.md

# 2. Синхронизируйте
npm run sync

# 3. Проверьте в браузере (если npm start запущен)
# Файл должен появиться в навигации
```

### Способ 2: Напрямую в docs/

```bash
# 1. Создайте файл в docs/
cat > docs/new-guide.md << 'EOF'
---
title: Новый гайд
sidebar_position: 10
---

# Новый гайд

Содержание гайда здесь...
EOF

# 2. Docusaurus автоматически подхватит (hot reload)
```

### Способ 3: Watch режим (для активной работы)

```bash
# 1. Запустите watch режим
npm run dev:watch

# 2. Создавайте файлы где угодно
echo "# Тестовый документ" > test-doc.md

# 3. Автоматически появляется в Docusaurus!
```

---

## 👥 Настройка команды

### Шаг 1: Пригласите участников

1. Settings → Collaborators
2. Add people
3. Введите GitHub username'ы
4. Выберите роль:
   - **Write** - для обычных участников
   - **Admin** - для техлида

### Шаг 2: Обучите команду Git

Проведите воркшопы:

```bash
# Воркшоп 1: Git Basics (30 минут)
# Используйте docs/workflows/git-basics.md

# Воркшоп 2: Git Branches (45 минут)
# Используйте docs/workflows/git-branches.md

# Воркшоп 3: Team Workflow (30 минут)
# Используйте docs/workflows/team-workflow.md
```

### Шаг 3: Первые PR от всех

Каждый член команды должен:

```bash
# 1. Клонировать репо
git clone https://github.com/ваш-username/high-time-workflow.git

# 2. Создать ветку
git checkout -b my-first-contribution

# 3. Создать файл
echo "# Моя первая страница" > my-page.md

# 4. Закоммитить
git add .
git commit -m "Моя первая страница"

# 5. Запушить
git push -u origin my-first-contribution

# 6. Создать PR на GitHub
```

### Шаг 4: Распределите ownership

Обновите `.github/CODEOWNERS`:

```
# Продакт-менеджер владеет уроками
/docs/lessons/lesson-0.md @product-manager-username
/docs/lessons/lesson-1.md @product-manager-username

# Маркетолог владеет lesson-2
/docs/lessons/lesson-2.md @marketer-username

# РОП владеет lesson-3
/docs/lessons/lesson-3.md @sales-lead-username

# Техлид владеет техническими файлами
/docusaurus.config.js @tech-lead-username
/.github/* @tech-lead-username

# Дизайнер владеет визуальными материалами
/static/img/* @designer-username
```

---

## ✅ Финальный чек-лист

### Репозиторий готов
- [ ] Создан на GitHub
- [ ] Склонирован локально
- [ ] Все папки созданы
- [ ] Все файлы скопированы
- [ ] package.json обновлён
- [ ] .gitignore настроен

### Docusaurus работает
- [ ] npm install выполнен успешно
- [ ] npm start запускает сайт
- [ ] Навигация работает
- [ ] Контент отображается
- [ ] Стили применяются

### Автосинхронизация настроена
- [ ] sync-filesystem.js скопирован
- [ ] npm run sync работает
- [ ] npm run dev:watch работает
- [ ] Новые файлы автоматически появляются

### GitHub настроен
- [ ] GitHub Pages включён
- [ ] GitHub Actions права настроены
- [ ] CODEOWNERS обновлён с реальными username
- [ ] Templates скопированы
- [ ] Первый деплой успешен

### Команда готова
- [ ] Все участники добавлены
- [ ] Git воркшопы проведены
- [ ] Каждый сделал первый PR
- [ ] Ownership распределён
- [ ] Telegram/Slack чат создан

### Документация актуальна
- [ ] README.md обновлён
- [ ] SETUP.md проверен
- [ ] Контакты и ссылки корректны
- [ ] Примеры работают

---

## 🎯 Что дальше?

### Неделя 1: Стабилизация
- [ ] Все делают тестовые PR
- [ ] Исправляете найденные проблемы
- [ ] Собираете фидбек от команды
- [ ] Дописываете недостающую документацию

### Неделя 2: Контент
- [ ] Добавляете реальный контент уроков
- [ ] Переносите существующие материалы
- [ ] Настраиваете структуру под ваши нужды
- [ ] Добавляете изображения и примеры

### Неделя 3: Оптимизация
- [ ] Настраиваете поиск (Algolia)
- [ ] Добавляете analytics
- [ ] Оптимизируете производительность
- [ ] Настраиваете SEO

### Неделя 4: Production ready
- [ ] Создаёте первую версию (v1.0)
- [ ] Анонсируете команде
- [ ] Собираете metrics
- [ ] Планируете улучшения

---

## 🆘 Troubleshooting

### Проблема: npm install падает

**Решение:**
```bash
# Очистите кеш
npm cache clean --force

# Удалите node_modules
rm -rf node_modules package-lock.json

# Переустановите
npm install
```

### Проблема: npm start не работает

**Решение:**
```bash
# Проверьте Node.js версию
node --version
# Должно быть >= 18.0

# Попробуйте очистить
npm run clear

# Переустановите зависимости
rm -rf node_modules
npm install
```

### Проблема: GitHub Pages показывает 404

**Решение:**
1. Проверьте что ветка `gh-pages` создалась
2. Settings → Pages → проверьте настройки
3. Подождите 5-10 минут
4. Проверьте `baseUrl` в `docusaurus.config.js`

### Проблема: Автосинхронизация не работает

**Решение:**
```bash
# Проверьте что файл существует
ls -la sync-filesystem.js

# Проверьте что chokidar установлен
npm list chokidar

# Переустановите если нужно
npm install chokidar --save-dev

# Попробуйте запустить вручную
node sync-filesystem.js
```

---

## 📚 Дополнительные ресурсы

### Документация
- [Docusaurus Docs](https://docusaurus.io/docs)
- [GitHub Pages Docs](https://pages.github.com/)
- [GitHub Actions Docs](https://docs.github.com/en/actions)

### Гайды в проекте
- `SETUP.md` - Детальная установка
- `SYSTEM.md` - Полная система
- `AUTO-SYNC-GUIDE.md` - Автосинхронизация
- `docs/workflows/git-basics.md` - Git для начинающих

### Полезные команды
```bash
# Docusaurus
npm start              # Dev server
npm run build         # Production build
npm run serve         # Test production build

# Sync
npm run sync          # Ручная синхронизация
npm run dev:watch     # Dev с автосинхронизацией

# Git
git status            # Проверить статус
git log --oneline     # История коммитов
git branch -a         # Все ветки
```

---

## 🎉 Поздравляю!

Вы создали полноценный репозиторий с:
- ✅ Docusaurus документацией
- ✅ Автосинхронизацией файлов
- ✅ CI/CD деплоем
- ✅ Системой workflow
- ✅ Обучающими материалами
- ✅ Готовой командной работой

**Теперь можно работать! 🚀**

---

_Последнее обновление: 24 декабря 2024_  
_Версия: 1.0_  
_Автор: Amoeba164_
