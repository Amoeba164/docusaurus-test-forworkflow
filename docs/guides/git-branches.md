# Git Branches: Работа с ветками и Pull Requests

## 🎯 Зачем нужны ветки?

Представьте: вы работаете над большим изменением. Это займёт несколько дней. Но что если в середине процесса:
- Коллега нашёл критичный баг
- Нужно срочно что-то обновить
- Ваши изменения ещё не готовы

**Проблема:** Если работать в `main`, вы заблокируете всех!

**Решение:** Работайте в своей ветке, а когда готово - вольёте в `main`.

---

## 📖 Ключевые концепции

### Main ветка
- Основная ветка проекта
- Всегда должна быть рабочей
- Деплоится на production
- **Правило:** Не ломайте main!

### Feature ветка
- Временная ветка для вашей работы
- Называется по задаче: `feature/add-examples`
- Можно экспериментировать
- Когда готово → Pull Request → Merge в main

---

## 🚀 Workflow с ветками

### 1️⃣ Создать ветку

```bash
# Убедитесь что вы в main и всё обновлено
git checkout main
git pull

# Создайте новую ветку
git checkout -b feature/add-test-lesson-0

# Что произошло:
# - Создана новая ветка
# - Вы автоматически переключились на неё
```

**Naming conventions (как называть ветки):**
- `feature/` - новая функциональность
- `fix/` - исправление бага
- `docs/` - документация
- `refactor/` - рефакторинг

**Примеры:**
- ✅ `feature/add-interactive-test`
- ✅ `fix/typo-lesson-0`
- ✅ `docs/update-workflow`
- ❌ `my-branch`
- ❌ `test`
- ❌ `asdf`

---

### 2️⃣ Работать в ветке

```bash
# Проверьте в какой ветке вы
git branch

# Должно показать что-то вроде:
#   main
# * feature/add-test-lesson-0    ← звёздочка = вы здесь

# Теперь работайте как обычно
# ... редактируйте файлы ...

git add .
git commit -m "Добавил интерактивный тест"
```

**Важно:** Все коммиты идут в ВАШУ ветку, main не трогается!

---

### 3️⃣ Отправить ветку на GitHub

```bash
# Первый раз для этой ветки
git push -u origin feature/add-test-lesson-0

# Последующие разы просто
git push
```

---

### 4️⃣ Создать Pull Request

1. Идите на GitHub в ваш репозиторий
2. Увидите жёлтый баннер "Compare & pull request" - кликните
3. Заполните описание:
   - Что изменили
   - Зачем
   - Скриншоты если нужно
4. Нажмите "Create pull request"

**Что произойдёт:**
- CODEOWNERS автоматически добавит ревьюера
- Запустятся проверки (GitHub Actions)
- Owner получит уведомление

---

### 5️⃣ Ревью и исправления

Ревьюер оставит комментарии. Если нужны правки:

```bash
# Вы всё ещё в своей ветке
# ... исправляете ...

git add .
git commit -m "Исправил замечания ревью"
git push

# Изменения автоматически добавятся в PR!
```

---

### 6️⃣ Merge

Когда PR одобрен:
1. Owner нажимает "Merge pull request"
2. Ваши изменения вливаются в main
3. Ветка удаляется
4. GitHub Actions деплоит на production

---

## 🎯 Полный workflow пример

```bash
# ========================================
# Шаг 1: Подготовка
# ========================================
git checkout main                    # Переключиться на main
git pull                            # Обновиться

# ========================================
# Шаг 2: Создание ветки
# ========================================
git checkout -b feature/add-examples

# ========================================
# Шаг 3: Работа (может занять дни)
# ========================================
# ... редактируем файлы ...
git add .
git commit -m "Добавил примеры"

# ... ещё правки ...
git add .
git commit -m "Улучшил форматирование"

# ... ещё правки ...
git add .
git commit -m "Финальные штрихи"

# ========================================
# Шаг 4: Отправка на GitHub
# ========================================
git push -u origin feature/add-examples

# ========================================
# Шаг 5: Pull Request на GitHub
# ========================================
# Создать PR через интерфейс GitHub

# ========================================
# Шаг 6: Ревью и правки
# ========================================
# ... ревьюер оставил комментарии ...
# ... исправляем ...
git add .
git commit -m "Исправил замечания"
git push

# ========================================
# Шаг 7: Merge (делает ревьюер)
# ========================================
# PR merged → ветка удалена

# ========================================
# Шаг 8: Обновить локальный main
# ========================================
git checkout main
git pull

# Готово! Можете создавать новую ветку
```

---

## 🔄 Полезные команды

### Посмотреть все ветки
```bash
git branch              # Локальные ветки
git branch -a          # Все ветки (включая remote)
```

### Переключиться между ветками
```bash
git checkout main                    # На main
git checkout feature/my-feature     # На feature
```

### Удалить ветку
```bash
# После merge, локально
git branch -d feature/my-feature

# Если нужно удалить force
git branch -D feature/my-feature
```

### Синхронизировать с main
```bash
# Вы в feature ветке
# Main обновился, хотите получить изменения

git checkout main
git pull
git checkout feature/my-feature
git merge main

# Или через rebase (продвинутый способ)
git checkout feature/my-feature
git rebase main
```

---

## 🆘 Частые проблемы

### ❌ "Cannot switch branches, you have uncommitted changes"

**Решение 1:** Закоммитьте изменения
```bash
git add .
git commit -m "WIP: промежуточные изменения"
git checkout other-branch
```

**Решение 2:** Спрячьте изменения (stash)
```bash
git stash
git checkout other-branch
# ... поработали ...
git checkout back-to-original
git stash pop
```

---

### ❌ Забыл создать ветку, закоммитил в main

**Решение:**
```bash
# Создайте ветку с текущими коммитами
git branch feature/my-work

# Откатите main назад
git reset --hard origin/main

# Переключитесь на новую ветку
git checkout feature/my-work

# Теперь запушьте
git push -u origin feature/my-work
```

---

### ❌ Конфликт при merge

**Что случилось:** Вы и кто-то ещё изменили одни и те же строки.

**Решение:**
```bash
# Git пометит конфликтные файлы
git status                # Посмотрите что конфликтует

# Откройте файлы в VS Code
# Увидите маркеры:
# <<<<<<< HEAD
# Ваша версия
# =======
# Их версия
# >>>>>>> main

# Выберите нужный вариант или объедините
# Удалите маркеры
# Сохраните файл

git add .
git commit -m "Разрешил конфликты"
```

---

## 📝 Best Practices

### ✅ DO:
- Создавайте ветку для каждой задачи
- Давайте понятные имена веткам
- Делайте маленькие коммиты с понятными сообщениями
- Регулярно синхронизируйтесь с main
- Удаляйте ветки после merge

### ❌ DON'T:
- Не работайте долго в одной ветке (>1 недели)
- Не создавайте ветки от веток (branch from branch)
- Не делайте гигантские PR (>500 строк)
- Не коммитьте всё в один коммит
- Не забывайте pull перед созданием ветки

---

## 🎓 Практика

### Упражнение 1: Первая ветка и PR

```bash
# 1. Создайте ветку
git checkout -b practice/my-first-pr

# 2. Создайте файл
echo "# Моя практика" > docs/practice.md

# 3. Закоммитьте
git add .
git commit -m "Добавил файл для практики"

# 4. Запушьте
git push -u origin practice/my-first-pr

# 5. Создайте PR на GitHub
# 6. Попросите кого-то сделать ревью
# 7. Смержите
# 8. Удалите ветку
```

---

### Упражнение 2: Исправления по ревью

```bash
# 1. Создайте PR (как в упражнении 1)
# 2. Попросите оставить комментарии
# 3. Исправьте замечания
# 4. Закоммитьте и запушьте
# 5. Убедитесь что изменения появились в PR
```

---

### Упражнение 3: Работа с конфликтами

```bash
# 1. Создайте ветку
git checkout -b practice/conflict

# 2. Измените строку в файле
echo "Моя версия" > docs/test.md
git add .
git commit -m "Моя версия"

# 3. Вернитесь в main, измените ту же строку
git checkout main
echo "Другая версия" > docs/test.md
git add .
git commit -m "Другая версия"

# 4. Попробуйте смержить
git checkout practice/conflict
git merge main
# Получите конфликт!

# 5. Разрешите конфликт в VS Code
# 6. Закоммитьте
git add .
git commit -m "Разрешил конфликт"
```

---

## ✅ Чек-лист "Я освоил ветки"

- [ ] Понимаю зачем нужны ветки
- [ ] Умею создавать ветку
- [ ] Умею переключаться между ветками
- [ ] Умею делать коммиты в ветке
- [ ] Умею пушить ветку на GitHub
- [ ] Создал хотя бы один PR
- [ ] Прошёл процесс ревью
- [ ] Смержил свой PR
- [ ] Решил конфликт хотя бы раз
- [ ] Синхронизировал ветку с main

---

## 🎯 Что дальше?

Когда освоите ветки, переходите к [Git Advanced](git-advanced.md) чтобы изучить:
- Rebase vs Merge
- Cherry-pick
- Git hooks
- Продвинутые техники

---

## 🆘 Нужна помощь?

- Telegram чат команды
- GitHub Discussions в репозитории
- [Git Branching Tutorial](https://learngitbranching.js.org/)

---

**Помните:** Ветки - это безопасность! Экспериментируйте смело! 💪
