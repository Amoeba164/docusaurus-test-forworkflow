# Git Basics: Гайд для начинающих

## 🎯 Цель этого гайда

За 30 минут научиться делать базовые операции с Git, достаточные для работы над документацией.

## 📚 Что такое Git?

Git - это система контроля версий. Представьте:
- Вы пишете документ
- Сохраняете черновик 1, черновик 2, финал...
- Хотите вернуться к черновику 1
- Хотите видеть что изменилось между версиями

Git делает это автоматически для всей команды!

## 🔧 Установка

### Mac:
```bash
# Проверьте есть ли Git
git --version

# Если нет, установите через Homebrew
brew install git
```

### Windows:
Скачайте с https://git-scm.com/download/win

### Linux:
```bash
sudo apt-get install git
```

## 🚀 Первые шаги (делаем один раз)

### 1. Настройте имя и email

```bash
git config --global user.name "Ваше Имя"
git config --global user.email "your-email@example.com"
```

**Важно:** Email должен совпадать с вашим GitHub аккаунтом!

### 2. Настройте редактор (опционально)

```bash
# VS Code как редактор для коммитов
git config --global core.editor "code --wait"
```

## 📖 5 команд которые нужно знать

### 1️⃣ git clone - Скачать репозиторий

```bash
# Перейдите в папку где хотите работать
cd ~/Documents

# Склонируйте репозиторий
git clone https://github.com/Amoeba164/high-time-workflow.git

# Зайдите в папку
cd high-time-workflow
```

**Что произошло:** Вы скачали весь проект на свой компьютер.

---

### 2️⃣ git pull - Обновить свою версию

```bash
# Перед началом работы ВСЕГДА делайте pull
git pull
```

**Что произошло:** Скачали последние изменения от других участников команды.

**Когда использовать:** Каждый раз перед началом работы!

---

### 3️⃣ git add - Подготовить файлы к сохранению

```bash
# Добавить все изменённые файлы
git add .

# Или конкретный файл
git add docs/lessons/lesson-0.md
```

**Что произошло:** Git теперь знает что эти файлы нужно сохранить.

---

### 4️⃣ git commit - Сохранить изменения

```bash
# Сохранить с сообщением
git commit -m "Исправил опечатку в уроке 0"
```

**Что произошло:** Создали "снимок" ваших изменений с описанием.

**Хорошие сообщения коммитов:**
- ✅ "Добавил примеры в урок 2"
- ✅ "Исправил опечатки в анализе"
- ✅ "Обновил workflow документацию"

**Плохие сообщения:**
- ❌ "fix"
- ❌ "update"
- ❌ "asdfasdf"

---

### 5️⃣ git push - Отправить на GitHub

```bash
# Отправить свои изменения
git push
```

**Что произошло:** Ваши изменения теперь на GitHub, все их видят!

---

## 🎯 Типичный workflow (каждый день)

```bash
# 1. Обновляемся
git pull

# 2. Редактируем файлы в VS Code
#    ... работаем ...

# 3. Смотрим что изменили
git status

# 4. Добавляем изменения
git add .

# 5. Сохраняем с сообщением
git commit -m "Описание того что сделали"

# 6. Отправляем на GitHub
git push
```

**Вот и всё! Это 90% вашей работы с Git.**

---

## 🆘 Частые проблемы

### ❌ Проблема: "Permission denied"

**Решение:** Настройте SSH ключи или используйте HTTPS с токеном.

```bash
# Проверьте URL репозитория
git remote -v

# Если начинается с git@, нужен SSH ключ
# Если с https://, нужен токен
```

---

### ❌ Проблема: "Your local changes would be overwritten"

**Что случилось:** Вы изменили файл, а кто-то тоже его изменил.

**Решение 1:** Сохраните свои изменения
```bash
git stash        # Спрятать свои изменения
git pull         # Обновиться
git stash pop    # Вернуть свои изменения
```

**Решение 2:** Отмените свои изменения (если они не важны)
```bash
git reset --hard
git pull
```

---

### ❌ Проблема: "Failed to push"

**Что случилось:** Кто-то запушил изменения раньше вас.

**Решение:**
```bash
git pull         # Сначала обновитесь
git push         # Теперь пушьте
```

---

## 📝 Полезные команды

### Посмотреть что изменено
```bash
git status              # Список изменённых файлов
git diff                # Детальные изменения
```

### Отменить изменения
```bash
git checkout filename   # Отменить изменения в файле
git reset --hard       # Отменить ВСЕ изменения (осторожно!)
```

### История
```bash
git log                 # История коммитов
git log --oneline      # Краткая история
```

---

## 🎓 Практика

### Упражнение 1: Первый коммит

1. Откройте любой .md файл в VS Code
2. Измените что-нибудь (добавьте пробел)
3. Сохраните файл
4. Выполните:
```bash
git status              # Увидите изменённый файл
git add .
git commit -m "Моя первая практика"
git push
```

5. Проверьте на GitHub - ваш коммит должен появиться!

---

### Упражнение 2: Исправить опечатку

1. Найдите опечатку в любом документе (или специально создайте)
2. Исправьте
3. Закоммитьте с понятным сообщением
4. Запушьте
5. Откройте GitHub и посмотрите свой коммит

---

## ✅ Чек-лист "Я освоил Git Basics"

- [ ] Понимаю зачем нужен Git
- [ ] Настроил имя и email
- [ ] Могу склонировать репозиторий
- [ ] Могу сделать pull
- [ ] Могу сделать add, commit, push
- [ ] Знаю как посмотреть статус
- [ ] Знаю базовые решения проблем
- [ ] Сделал 2 успешных коммита

---

## 🎯 Что дальше?

Когда освоите эти 5 команд, переходите к [Git Branches](git-branches.md) чтобы научиться:
- Создавать ветки
- Работать с Pull Requests
- Не ломать main ветку

---

## 🆘 Нужна помощь?

- Спросите в Telegram чате команды
- Создайте Issue с тегом `[вопрос]`
- Посмотрите [Git Cheat Sheet](https://education.github.com/git-cheat-sheet-education.pdf)

---

**Помните:** Все когда-то были новичками в Git. Не бойтесь ошибаться!

**Git позволяет откатить любые изменения** - вы не сломаете проект! 💪
